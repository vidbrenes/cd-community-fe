import StickyNotesWrapper from '@/components/StickyNotes/StickyNotesWrapper';

export default function NotesPage() {
  return (
    <main className="relative min-h-screen overflow-scroll">
      <StickyNotesWrapper />
    </main>
  )
}